# Backfill dry-run output — 2026-05-17

`tsx scripts/backfill-agent-pr-urls.ts --db /home/node/.openclaw/workspace/autodev-data/orchestrator.db --dry-run`

Ran against the live autodev orchestrator DB on the openclaw gateway container
(`/home/node/.openclaw/workspace/autodev-data/orchestrator.db`).

- 21 completed workflows had `pr_url IS NULL`.
- 4 had a candidate `https://github.com/.../pull/N` URL in the most recent done
  implementer agent's output whose owner/repo matched the workflow's resolved
  GitHub origin (via the M6 `work_dir` fallback — these worktrees no longer
  have a usable `origin` remote of their own).
- Full `gh pr view` validation could not run from this container because
  `gh auth` is not configured here, so candidates surface as `manual-review`
  with `validation = matches origin (gh unverified)`. The script never writes
  in dry-run mode, and `--apply` mode still requires the full validator (PR
  state `OPEN` AND `headRefName == workflow.worktree_branch`).
- M10 must re-run this on a host with `gh auth` and run `--apply` only after
  the operator explicitly approves the candidate table below.

## Candidate URLs (manual-review)

| workflow id                          | branch                                                     | candidate URL                                       |
|--------------------------------------|------------------------------------------------------------|-----------------------------------------------------|
| f3718289-203c-485d-bb9f-43f7bf4d029f | workflow/polymarket-agent-ml-v2-2-fix-label-resol-f3718289 | https://github.com/bh13731/polymarket-agent/pull/27 |
| 44f28b8c-280d-4627-b197-69702c19e313 | workflow/polymarket-agent-ml-v2-3b-sourceable-fea-44f28b8c | https://github.com/bh13731/polymarket-agent/pull/28 |
| 714c07ad-dc33-44ec-808c-3f2cbab717f1 | workflow/polymarket-agent-ml-v2-4-fix-sharpe-nan--714c07ad | https://github.com/bh13731/polymarket-agent/pull/30 |
| 112c728c-7dbc-42dd-8aad-607f567ed8be | workflow/polymarket-agent-zoo-v3-bump-timeout-fir-112c728c | https://github.com/bh13731/polymarket-agent/pull/29 |

## Skipped (no candidate)

17 of the 21 completed-with-NULL-pr_url workflows had no URL in their most
recent done implementer agent's output that matched origin. These will not be
touched even by `--apply` because the finder returns null.

## Cross-check against the brief

- The brief named `714c07ad` → PR #30, `112c728c` → PR #29, `c8cace7d` →
  PR #31 (rescued by hand). The candidate table confirms the first two
  programmatically and additionally surfaces `f3718289` → #27 and
  `44f28b8c` → #28, which the brief did not mention by id but which appear in
  the agent output trail of cycles around the 2026-05-17 incident.
- `c8cace7d` is correctly **not** surfaced here. PR #31 was rescued by a
  human operator on 2026-05-17, but the workflow's most recent done
  implementer agent never ran `gh pr create` itself, so there is no URL in
  agent output for the finder to match. Operator-rescued PRs are out of scope
  for this safety-net path.

## How to run

Dry-run:
```
NODE_OPTIONS='--experimental-sqlite' tsx scripts/backfill-agent-pr-urls.ts \
  --db /home/node/.openclaw/workspace/autodev-data/orchestrator.db --dry-run
```

Apply (after explicit operator approval and from a host with `gh auth`):
```
NODE_OPTIONS='--experimental-sqlite' tsx scripts/backfill-agent-pr-urls.ts \
  --db /home/node/.openclaw/workspace/autodev-data/orchestrator.db --apply
```
